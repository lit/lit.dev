import { LitElement, html } from 'lit-element';

/**
 * TODO: Implmenent the component 
 */
class MyElement extends LitElement { }

/**
 * Register the new element with the browser.
 */
customElements.define('my-element', MyElement);

