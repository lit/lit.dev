{
  "name": "lit.dev",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo root package for lit.dev",
  "author": "Google LLC",
  "license": "BSD-3-Clause",
  "scripts": {
    "bootstrap": "lerna bootstrap --ci",
    "build": "lerna run build --stream",
    "start": "cd packages/lit-dev-server && npm start",
    "dev": "cd packages/lit-dev-content && (npm run build:ts:watch -- --preserveWatchOutput & npm run dev:build:site:watch & npm run dev:serve)",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "nuke": "rm -rf node_modules && npm ci && lerna exec 'rm -rf node_modules' && lerna bootstrap --ci",
    "upgrade": "rm -rf node_modules package-lock.json && npm install && lerna exec 'rm -rf node_modules package-lock.json' && lerna bootstrap",
    "test:unit": "lerna run test --scope lit-dev-tools-cjs --scope lit-dev-tools-esm",
    "test:integration": "lerna run test:integration",
    "test:integration:update-golden-screenshots": "lerna run test:integration:update-golden-screenshots",
    "test:links:redirects": "lerna run test:links:redirects",
    "test:links:internal": "lerna run test:links:internal",
    "test:links:external": "lerna run test:links:external"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.1.2",
    "typescript": "^4.4.3"
  }
}
